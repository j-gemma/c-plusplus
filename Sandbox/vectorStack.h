#pragma once
#include <iostream>
#include <vector>

int pushValue(int values, std::vector<int>& stack);

int popValue(std::vector<int>& stack);

int printStack(std::vector<int>& stack);

int runVectorStack();